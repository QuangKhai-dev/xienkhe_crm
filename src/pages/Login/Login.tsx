import React from 'react';
import TextField from '@mui/material/TextField';
type Props = {};

const Login = (props: Props) => {
  return <div className="bg-login-theme h-screen"></div>;
};

export default Login;
